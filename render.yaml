services:
  - type: web
    name: joytopia2-game
    runtime: static
    buildCommand: echo "Joytopia 2 static build - LESC scripts are precompiled"
    staticPublishPath: .
    pullRequestPreviewsEnabled: false
    headers:
      - path: /*
        name: Cache-Control
        value: no-cache
